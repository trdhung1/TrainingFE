{
  "name": "employee-management",
  "version": "1.0.0",
  "author": "Dung Nguyen",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.17.3"
  },
  "scripts": {
    "start": "concurrently \"json-server --watch db.json --port 8081\" \"json-server --watch user.json --port 8082\""
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
