{
  "name": "employeeManager",
  "version": "1.0.0",
  "author": "Dung Nguyen",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.17.3"
  },
  "scripts": {
    "start1": "json-server --watch db.json --port 8081",
    "start2": "json-server --watch user.json --port 8082"
  }
}
